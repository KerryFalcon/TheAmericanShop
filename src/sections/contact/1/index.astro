<!--
  CONTACT — Pop Urban × White Light
-->

<section class="contact-one" id="contact">
  <div class="checker-strip-top" aria-hidden="true"></div>
  <div class="splash sc1" aria-hidden="true"></div>
  <div class="splash sc2" aria-hidden="true"></div>

  <div class="contact-header">
    <span class="eyebrow">✦ Contáctanos</span>
    <h2>¿Tienes alguna pregunta o pedido especial?</h2>
    <p class="intro">
      Escríbenos y te ayudamos a encontrar tus snacks favoritos. También hacemos pedidos especiales
      si no encuentras lo que buscas.
    </p>
  </div>

  <div class="wrapper">
    <div class="info-items">
      <div class="info-item">
        <div class="info-icon">📧</div>
        <div>
          <h3>Email</h3>
          <p>hola@theamericanshop.mx</p>
        </div>
      </div>
      <div class="info-item">
        <div class="info-icon">📱</div>
        <div>
          <h3>WhatsApp</h3>
          <p>+52 (123) 456-7890</p>
        </div>
      </div>
      <div class="info-item">
        <div class="info-icon">📍</div>
        <div>
          <h3>Ubicación</h3>
          <p>Envíos a todo México</p>
        </div>
      </div>
    </div>

    <form class="form" action="#" method="POST">
      <div class="field">
        <label for="contact-name">Tu Nombre</label>
        <input type="text" id="contact-name" name="name" placeholder="¿Cómo te llamas?" />
      </div>
      <div class="field">
        <label for="contact-email">Tu Email</label>
        <input type="email" id="contact-email" name="email" placeholder="tu@email.com" />
      </div>
      <div class="field">
        <label for="contact-message">¿Qué necesitas?</label>
        <textarea id="contact-message" name="message" rows="4" placeholder="Cuéntanos qué productos buscas..."></textarea>
      </div>
      <button type="submit">Enviar Mensaje →</button>
    </form>
  </div>

  <div class="checker-strip-bottom" aria-hidden="true"></div>
</section>

<style>
  .contact-one {
    position: relative;
    overflow: hidden;
    padding: clamp(4rem, 7vw, 6rem) clamp(2rem, 6vw, 6rem);
    padding-top: calc(clamp(4rem, 7vw, 6rem) + 32px);
    padding-bottom: calc(clamp(4rem, 7vw, 6rem) + 32px);
    color: var(--text-dark, #1a1a2e);
    background:
      radial-gradient(ellipse 50% 40% at 20% 20%, rgba(0,216,255,0.06), transparent 50%),
      radial-gradient(ellipse 60% 50% at 85% 80%, rgba(255,47,179,0.06), transparent 50%),
      linear-gradient(180deg, #fdf0f7 0%, #ffffff 40%, #f5f0ff 100%);
  }

  .splash { position: absolute; border-radius: 50%; filter: blur(100px); pointer-events: none; z-index: 0; }
  .sc1 { width: 350px; height: 350px; top: 5%; right: -3%; background: rgba(255,47,179,0.1); }
  .sc2 { width: 280px; height: 280px; bottom: 5%; left: 5%; background: rgba(0,216,255,0.08); }

  .contact-header {
    text-align: center;
    max-width: 650px;
    margin: 0 auto 3rem;
    position: relative;
    z-index: 2;
  }

  .eyebrow {
    display: inline-block;
    margin: 0 0 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    font-size: 0.75rem;
    font-weight: 800;
    color: var(--neon-pink, #ff2fb3);
    background: rgba(255,47,179,0.08);
    padding: 0.35rem 1rem;
    border-radius: 999px;
    border: 1px solid rgba(255,47,179,0.2);
  }

  h2 {
    margin: 0.5rem 0;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.05;
    color: var(--text-dark, #1a1a2e) !important;
    text-shadow:
      0 0 8px rgba(255,47,179,0.3),
      0 0 20px rgba(255,47,179,0.15);
  }

  .intro {
    margin: 0;
    color: #555;
    line-height: 1.7;
    font-size: 1.05rem;
  }

  .wrapper {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.2fr);
    gap: 2.5rem;
    align-items: start;
    max-width: 1100px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  .info-items {
    display: grid;
    gap: 1rem;
  }

  .info-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.2rem;
    border: 2px solid rgba(255,47,179,0.15);
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 4px 20px rgba(0,0,0,0.04);
    transition: border-color 0.3s, box-shadow 0.3s, transform 0.3s;
  }
  .info-item:hover {
    border-color: var(--neon-pink, #ff2fb3);
    box-shadow: 0 8px 30px rgba(255,47,179,0.1);
    transform: translateY(-3px);
  }

  .info-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
    width: 44px; height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255,47,179,0.08);
    border-radius: 12px;
  }

  .info-item h3 {
    margin: 0;
    font-size: 0.9rem;
    font-weight: 800;
    color: var(--neon-pink, #ff2fb3);
  }

  .info-item p {
    margin: 0.15rem 0 0;
    color: #666;
    font-size: 0.9rem;
  }

  .form {
    display: grid;
    gap: 1.1rem;
    background: #fff;
    border: 2px solid rgba(0,216,255,0.15);
    border-radius: 24px;
    padding: 2.2rem;
    box-shadow: 0 8px 40px rgba(0,0,0,0.06);
    transition: border-color 0.3s, box-shadow 0.3s;
  }
  .form:focus-within {
    border-color: var(--neon-pink, #ff2fb3);
    box-shadow: 0 8px 40px rgba(255,47,179,0.08);
  }

  .field {
    display: grid;
    gap: 0.4rem;
  }

  label {
    font-size: 0.85rem;
    font-weight: 700;
    color: #333;
    letter-spacing: 0.02em;
  }

  input,
  textarea {
    width: 100%;
    background: #f8f5ff;
    border: 2px solid #e8e4f0;
    border-radius: 14px;
    padding: 0.8rem 1rem;
    color: var(--text-dark, #1a1a2e);
    font-family: inherit;
    font-size: 0.95rem;
    outline: none;
    transition: border-color 0.3s, box-shadow 0.3s;
  }

  input:focus,
  textarea:focus {
    border-color: var(--neon-pink, #ff2fb3);
    box-shadow: 0 0 14px rgba(255,47,179,0.15);
    background: #fff;
  }

  textarea { resize: vertical; }

  button {
    background: linear-gradient(135deg, var(--neon-pink, #ff2fb3), #ff7ad6 50%, var(--neon-yellow, #f9f871));
    color: #0b0b12;
    border: none;
    border-radius: 999px;
    padding: 0.9rem 2rem;
    min-height: 46px;
    font-weight: 900;
    font-size: 1rem;
    letter-spacing: 0.02em;
    cursor: pointer;
    box-shadow: 0 4px 20px rgba(255,47,179,0.4), 0 0 40px rgba(249,248,113,0.15);
    transition: transform 0.2s, box-shadow 0.3s;
  }

  button:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 6px 30px rgba(255,47,179,0.6), 0 0 60px rgba(249,248,113,0.25);
  }

  @media (max-width: 768px) {
    .contact-one {
      padding: clamp(3.25rem, 12vw, 4.8rem) 1rem;
    }
    .contact-header {
      margin-bottom: 2rem;
    }
    .wrapper {
      grid-template-columns: 1fr;
      gap: 1.4rem;
    }
    .form {
      padding: 1.4rem;
      border-radius: 20px;
    }
  }

  @media (max-width: 430px) {
    .contact-one {
      padding-inline: 0.85rem;
    }
    h2 {
      font-size: clamp(1.9rem, 10vw, 2.6rem);
    }
    .info-item {
      padding: 1rem;
    }
    button {
      width: 100%;
    }
  }
</style>
