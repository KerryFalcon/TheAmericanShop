<!--
  PRICING — Pop Urban Neon × Contrast
-->

<section class="pricing-one" id="pricing">
  <div class="checker-strip-top" aria-hidden="true"></div>
  <div class="splash sp1" aria-hidden="true"></div>
  <div class="splash sp2" aria-hidden="true"></div>

  <div class="header">
    <span class="eyebrow">✦ Paquetes & Cajas</span>
    <h2>Arma tu caja de snacks americanos</h2>
    <p class="intro">Elige el paquete que más te guste. Todos incluyen productos 100% originales importados de USA.</p>
  </div>

  <div class="cards">
    <div class="card">
      <p class="plan">Snack Box</p>
      <p class="price">$349<span class="period">/caja</span></p>
      <ul>
        <li>5 productos a elegir</li>
        <li>Chips: Doritos, Cheetos, Funyuns</li>
        <li>1 bebida incluida</li>
        <li>Envío estándar gratis</li>
      </ul>
      <a href="#">Armar Caja</a>
    </div>

    <div class="card featured">
      <span class="badge">Popular</span>
      <div class="card-checker" aria-hidden="true"></div>
      <p class="plan">Mega Box</p>
      <p class="price">$699<span class="period">/caja</span></p>
      <ul>
        <li>12 productos a elegir</li>
        <li>Chips + Dulces + Bebidas</li>
        <li>Incluye 1 cereal importado</li>
        <li>Envío express gratis</li>
      </ul>
      <a href="#">Armar Mega Box</a>
    </div>

    <div class="card">
      <p class="plan">Party Box</p>
      <p class="price">$1,199<span class="period">/caja</span></p>
      <ul>
        <li>25 productos variados</li>
        <li>Mix completo de todo</li>
        <li>2 cereales + 4 bebidas</li>
        <li>Envío express + regalo sorpresa</li>
      </ul>
      <a href="#">Armar Party Box</a>
    </div>
  </div>

  <div class="checker-strip-bottom" aria-hidden="true"></div>
</section>

<style>
  .pricing-one {
    position: relative;
    overflow: hidden;
    padding: clamp(4rem, 7vw, 6rem) clamp(2rem, 6vw, 6rem);
    padding-top: calc(clamp(4rem, 7vw, 6rem) + 32px);
    padding-bottom: calc(clamp(4rem, 7vw, 6rem) + 32px);
    color: #fff;
    background:
      radial-gradient(ellipse 60% 50% at 50% 0%, rgba(255,47,179,0.1), transparent 50%),
      radial-gradient(ellipse 50% 40% at 80% 100%, rgba(0,216,255,0.08), transparent 50%),
      linear-gradient(180deg, #14102a 0%, #1a0a2e 50%, #12111a 100%);
  }

  .splash { position: absolute; border-radius: 50%; filter: blur(100px); pointer-events: none; z-index: 0; }
  .sp1 { width: 350px; height: 350px; top: -8%; right: 5%; background: rgba(0,216,255,0.15); }
  .sp2 { width: 300px; height: 300px; bottom: -5%; left: 15%; background: rgba(255,47,179,0.12); }

  .header {
    text-align: center;
    max-width: 650px;
    margin: 0 auto 3.5rem;
    position: relative;
    z-index: 2;
  }

  .eyebrow {
    display: inline-block;
    margin: 0 0 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    font-size: 0.75rem;
    font-weight: 800;
    color: var(--electric-blue, #00d8ff);
    background: rgba(0,216,255,0.08);
    padding: 0.35rem 1rem;
    border-radius: 999px;
    border: 1px solid rgba(0,216,255,0.25);
  }

  h2 {
    margin: 0.5rem 0;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.05;
  }

  .intro {
    margin: 0;
    color: rgba(255,255,255,0.6);
    font-size: 1.05rem;
  }

  .cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    max-width: 1060px;
    margin: 0 auto;
    align-items: start;
    position: relative;
    z-index: 2;
  }

  .card {
    background: rgba(255,255,255,0.06);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 2px solid rgba(255,255,255,0.12);
    border-radius: 24px;
    padding: 2.5rem 1.8rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s, border-color 0.3s, box-shadow 0.3s;
  }

  .card:hover {
    transform: translateY(-6px);
    border-color: rgba(0,216,255,0.4);
    box-shadow: 0 8px 40px rgba(0,216,255,0.12), 0 0 30px rgba(0,216,255,0.08);
  }

  .card.featured {
    background: rgba(255,255,255,0.95);
    color: var(--text-dark, #1a1a2e);
    border: 3px solid var(--neon-pink, #ff2fb3);
    transform: scale(1.06);
    box-shadow:
      0 0 40px rgba(255,47,179,0.25),
      0 16px 50px rgba(0,0,0,0.15);
  }
  .card.featured:hover {
    transform: scale(1.06) translateY(-6px);
    box-shadow:
      0 0 50px rgba(255,47,179,0.35),
      0 20px 60px rgba(0,0,0,0.2);
  }

  /* Checkered accent inside featured card */
  .card-checker {
    position: absolute;
    top: 0; left: 0; right: 0; height: 6px;
    background: url('/checkered-floor.svg') repeat-x;
    background-size: 12px 12px;
    opacity: 0.6;
  }

  .badge {
    position: absolute;
    top: -14px;
    left: 50%;
    transform: translateX(-50%);
    background: linear-gradient(135deg, var(--neon-pink, #ff2fb3), var(--neon-yellow, #f9f871));
    color: #0b0b12;
    font-size: 0.72rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    padding: 0.35rem 1.2rem;
    border-radius: 999px;
    box-shadow: 0 4px 16px rgba(255,47,179,0.4);
  }

  .plan {
    margin: 0;
    font-family: var(--font-display, 'Bangers', cursive);
    font-size: 1.1rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--electric-blue, #00d8ff);
  }
  .card.featured .plan {
    color: var(--neon-pink, #ff2fb3);
  }

  .price {
    margin: 0.3rem 0 1.2rem;
    font-family: var(--font-display, 'Bangers', cursive);
    font-size: 3.5rem;
    color: #fff;
  }
  .card.featured .price {
    color: var(--text-dark, #1a1a2e);
  }

  .period {
    font-family: var(--font-body, 'Inter', sans-serif);
    font-size: 0.9rem;
    font-weight: 500;
    color: rgba(255,255,255,0.4);
  }
  .card.featured .period {
    color: rgba(26,26,46,0.4);
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0 0 1.8rem;
    display: grid;
    gap: 0.6rem;
  }

  li {
    font-size: 0.9rem;
    color: rgba(255,255,255,0.6);
    padding: 0.5rem 0;
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
  .card.featured li {
    color: rgba(26,26,46,0.7);
    border-bottom-color: rgba(255,47,179,0.1);
  }

  .card a {
    display: inline-block;
    text-decoration: none;
    padding: 0.8rem 1.8rem;
    border-radius: 999px;
    font-weight: 800;
    font-size: 0.9rem;
    border: 2px solid rgba(255,255,255,0.2);
    color: #fff;
    background: rgba(255,255,255,0.05);
    transition: all 0.3s;
  }
  .card a:hover {
    border-color: var(--electric-blue, #00d8ff);
    background: rgba(0,216,255,0.1);
    box-shadow: 0 0 20px rgba(0,216,255,0.15);
  }

  .card.featured a {
    background: linear-gradient(135deg, var(--neon-pink, #ff2fb3), #ff7ad6 60%, var(--neon-yellow, #f9f871));
    color: #0b0b12;
    border: none;
    box-shadow: 0 4px 20px rgba(255,47,179,0.4);
  }
  .card.featured a:hover {
    box-shadow: 0 6px 30px rgba(255,47,179,0.6);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .cards { grid-template-columns: 1fr; max-width: 400px; }
    .card.featured { transform: none; }
  }
</style>
