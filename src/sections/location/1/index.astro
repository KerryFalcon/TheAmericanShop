<!--
  LOCATION — Pop Urban Neon Side-by-Side
-->

<section class="location-one" id="location">
  <div class="location-glow location-glow-pink" aria-hidden="true"></div>
  <div class="location-glow location-glow-blue" aria-hidden="true"></div>

  <div class="location-inner">
    <div class="location-header">
      <span class="eyebrow">✦ Ubicación</span>
      <h2>Visítanos</h2>
      <p class="intro">Encuentra The American Shop QRO y ven por tus dulces, snacks y productos favoritos.</p>
    </div>

    <div class="location-grid">
      <aside class="store-card">
        <header class="store-card-head">
          <span class="store-card-pin" aria-hidden="true">📍</span>
          <div>
            <small>Tienda oficial</small>
            <strong>The American Shop QRO</strong>
          </div>
        </header>

        <div class="store-divider" aria-hidden="true"></div>

        <ul class="store-meta">
          <li>
            <span class="store-meta-label">Dirección</span>
            <span class="store-meta-value">Querétaro, México</span>
          </li>
          <li>
            <span class="store-meta-label">Horario</span>
            <span class="store-meta-value">Lun a Sáb · 11:00 – 21:00</span>
          </li>
          <li>
            <span class="store-meta-label">Contacto</span>
            <span class="store-meta-value">Pedidos por WhatsApp</span>
          </li>
        </ul>

        <a class="store-cta" href="https://maps.app.goo.gl/?q=The+American+Shop+QRO" target="_blank" rel="noopener">
          <span>Cómo llegar</span>
          <svg viewBox="0 0 24 24" width="16" height="16" aria-hidden="true">
            <path d="M5 12h14M13 6l6 6-6 6" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </a>
      </aside>

      <div class="map-card">
        <span class="map-corner map-corner-tl" aria-hidden="true"></span>
        <span class="map-corner map-corner-tr" aria-hidden="true"></span>
        <span class="map-corner map-corner-bl" aria-hidden="true"></span>
        <span class="map-corner map-corner-br" aria-hidden="true"></span>
        <iframe src="https://www.google.com/maps/embed?pb=!1m14!1m8!1m3!1d119513.40460207526!2d-100.4658508!3d20.5964729!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x85d35b9c1cf79471%3A0xa5f9d2c93fab99f5!2sThe%20American%20Shop%20QRO!5e0!3m2!1sen!2smx!4v1779523791382!5m2!1sen!2smx" width="600" height="450" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>
    </div>
  </div>
</section>

<style>
  .location-one {
    position: relative;
    overflow: hidden;
    isolation: isolate;
    padding: clamp(4rem, 7vw, 6rem) clamp(1.25rem, 6vw, 6rem);
    color: #f0ece8;
    background:
      radial-gradient(ellipse 80% 60% at 30% 35%, rgba(255,47,179,0.12), transparent 56%),
      radial-gradient(ellipse 58% 48% at 78% 70%, rgba(0,216,255,0.09), transparent 52%),
      radial-gradient(ellipse 42% 36% at 18% 86%, rgba(249,248,113,0.05), transparent 52%),
      linear-gradient(180deg, #161228 0%, #181420 22%, #201828 55%, #1c1524 82%, #161228 100%);
  }

  /* Capa 1: contornos topográficos / radar */
  .location-one::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-image:
      repeating-radial-gradient(
        ellipse 70% 60% at 22% 28%,
        transparent 0,
        transparent 42px,
        rgba(255,47,179,0.06) 42px,
        rgba(255,47,179,0.06) 43px
      ),
      repeating-radial-gradient(
        ellipse 65% 55% at 78% 72%,
        transparent 0,
        transparent 38px,
        rgba(0,216,255,0.05) 38px,
        rgba(0,216,255,0.05) 39px
      );
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 12%, black 88%, transparent 100%);
    mask-image: linear-gradient(to bottom, transparent 0%, black 12%, black 88%, transparent 100%);
    animation: location-rings-pulse 9s ease-in-out infinite;
  }

  /* Capa 2: waypoints / pin dots dispersos con drift */
  .location-one::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-image:
      radial-gradient(circle at 16% 30%, rgba(255,47,179,0.7) 0, transparent 1.2px),
      radial-gradient(circle at 64% 18%, rgba(249,248,113,0.55) 0, transparent 1px),
      radial-gradient(circle at 88% 40%, rgba(0,216,255,0.65) 0, transparent 1.1px),
      radial-gradient(circle at 32% 78%, rgba(255,47,179,0.55) 0, transparent 0.9px),
      radial-gradient(circle at 78% 86%, rgba(0,216,255,0.55) 0, transparent 1px),
      radial-gradient(circle at 50% 50%, rgba(249,248,113,0.5) 0, transparent 0.9px);
    background-size: 320px 320px;
    background-repeat: repeat;
    animation: location-waypoints-drift 120s linear infinite;
    opacity: 0.75;
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 14%, black 86%, transparent 100%);
    mask-image: linear-gradient(to bottom, transparent 0%, black 14%, black 86%, transparent 100%);
  }

  @keyframes location-rings-pulse {
    0%, 100% { opacity: 0.65; }
    50% { opacity: 1; }
  }
  @keyframes location-waypoints-drift {
    from { background-position: 0 0; }
    to { background-position: 320px 320px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .location-one::before,
    .location-one::after,
    .location-glow { animation: none; }
  }

  .location-glow {
    position: absolute;
    z-index: 0;
    border-radius: 50%;
    filter: blur(100px);
    pointer-events: none;
    opacity: 0.25;
    will-change: transform, opacity;
    animation: location-breath 4.5s ease-in-out infinite;
  }
  .location-glow-pink { animation-duration: 4s; }
  .location-glow-blue { animation-duration: 5s; animation-delay: -2s; }

  @keyframes location-breath {
    0%, 100% { transform: scale(0.96); opacity: 0.2; }
    50% { transform: scale(1.2); opacity: 0.55; }
  }
  .location-glow-pink {
    width: 460px; height: 460px;
    top: 4%; right: -10%;
    background: rgba(255,47,179,0.55);
  }
  .location-glow-blue {
    width: 400px; height: 400px;
    bottom: 6%; left: -8%;
    background: rgba(0,216,255,0.5);
  }

  .location-inner {
    position: relative;
    z-index: 2;
    max-width: 1140px;
    width: 100%;
    margin: 0 auto;
  }

  .location-header {
    max-width: 700px;
    margin: 0 auto 2.5rem;
    text-align: center;
  }

  .eyebrow {
    display: inline-block;
    margin: 0 0 0.85rem;
    padding: 0.4rem 1rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    font-size: 0.75rem;
    font-weight: 800;
    color: var(--neon-pink, #ff2fb3);
    background: rgba(255,47,179,0.08);
    border: 1px solid rgba(255,47,179,0.28);
    border-radius: 999px;
    text-shadow: 0 0 6px rgba(255,47,179,0.35);
  }

  h2 {
    margin: 0.5rem 0;
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.05;
  }

  .intro {
    margin: 0;
    color: rgba(240,236,232,0.6);
    font-size: clamp(1rem, 2vw, 1.05rem);
    line-height: 1.7;
  }

  /* === LAYOUT === */
  .location-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.2fr);
    gap: 1.6rem;
    align-items: stretch;
  }

  /* === STORE CARD === */
  .store-card {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
    padding: 1.8rem 1.7rem;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 24px;
    backdrop-filter: blur(18px);
    -webkit-backdrop-filter: blur(18px);
    box-shadow:
      0 12px 36px rgba(0,0,0,0.4),
      0 0 28px rgba(255,47,179,0.08);
    overflow: hidden;
  }
  .store-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 14%;
    right: 14%;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(90deg, transparent, rgba(255,47,179,0.7), rgba(249,248,113,0.55), transparent);
    box-shadow: 0 0 14px rgba(255,47,179,0.5);
    opacity: 0.8;
  }

  .store-card-head {
    display: flex;
    align-items: center;
    gap: 0.95rem;
  }
  .store-card-pin {
    flex-shrink: 0;
    width: 52px;
    height: 52px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 1.4rem;
    background: linear-gradient(135deg, rgba(255,47,179,0.22), rgba(0,216,255,0.16));
    border: 1px solid rgba(255,47,179,0.45);
    border-radius: 18px;
    box-shadow:
      0 0 18px rgba(255,47,179,0.4),
      inset 0 0 16px rgba(0,216,255,0.1);
  }
  .store-card-head small {
    display: block;
    color: var(--neon-yellow, #f9f871);
    font-size: 0.66rem;
    font-weight: 900;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    margin-bottom: 0.2rem;
  }
  .store-card-head strong {
    display: block;
    color: #fff;
    font-size: 1.12rem;
    line-height: 1.2;
    letter-spacing: 0.01em;
    text-shadow: 0 0 8px rgba(255,47,179,0.25);
  }

  .store-divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.18), transparent);
  }

  .store-meta {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    flex: 1;
  }
  .store-meta li {
    display: flex;
    flex-direction: column;
    gap: 0.18rem;
  }
  .store-meta-label {
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--neon-pink, #ff2fb3);
    text-shadow: 0 0 6px rgba(255,47,179,0.35);
  }
  .store-meta-value {
    color: rgba(240,236,232,0.88);
    font-size: 0.95rem;
    font-weight: 600;
    line-height: 1.35;
  }

  .store-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.85rem 1.4rem;
    min-height: 46px;
    border-radius: 999px;
    background: linear-gradient(135deg, var(--neon-pink, #ff2fb3), #ff7ad6 50%, var(--neon-yellow, #f9f871));
    color: #0b0b12;
    text-decoration: none;
    font-weight: 900;
    font-size: 0.92rem;
    letter-spacing: 0.02em;
    box-shadow:
      0 6px 22px rgba(255,47,179,0.45),
      0 0 36px rgba(249,248,113,0.18);
    transition: transform 0.2s, box-shadow 0.3s;
  }
  .store-cta svg {
    transition: transform 0.25s ease;
  }
  .store-cta:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow:
      0 8px 30px rgba(255,47,179,0.6),
      0 0 50px rgba(249,248,113,0.28);
  }
  .store-cta:hover svg {
    transform: translateX(3px);
  }

  /* === MAP === */
  .map-card {
    position: relative;
    overflow: hidden;
    width: 100%;
    border: 1px solid rgba(240,236,232,0.1);
    border-radius: 24px;
    background: rgba(35,28,50,0.56);
    box-shadow:
      0 4px 24px rgba(0,0,0,0.36),
      0 0 26px rgba(255,47,179,0.12),
      0 0 36px rgba(0,216,255,0.06);
    transition: border-color 0.3s, box-shadow 0.3s, transform 0.3s;
  }
  .map-card:hover {
    border-color: rgba(255,47,179,0.3);
    box-shadow:
      0 8px 34px rgba(0,0,0,0.42),
      0 0 28px rgba(255,47,179,0.18),
      0 0 42px rgba(0,216,255,0.1);
    transform: translateY(-2px);
  }

  .map-corner {
    position: absolute;
    width: 28px;
    height: 28px;
    z-index: 2;
    pointer-events: none;
    opacity: 0.85;
  }
  .map-corner-tl {
    top: 14px; left: 14px;
    border-top: 2px solid var(--neon-pink, #ff2fb3);
    border-left: 2px solid var(--neon-pink, #ff2fb3);
    border-top-left-radius: 10px;
    filter: drop-shadow(0 0 6px rgba(255,47,179,0.55));
  }
  .map-corner-tr {
    top: 14px; right: 14px;
    border-top: 2px solid var(--electric-blue, #00d8ff);
    border-right: 2px solid var(--electric-blue, #00d8ff);
    border-top-right-radius: 10px;
    filter: drop-shadow(0 0 6px rgba(0,216,255,0.55));
  }
  .map-corner-bl {
    bottom: 14px; left: 14px;
    border-bottom: 2px solid var(--electric-blue, #00d8ff);
    border-left: 2px solid var(--electric-blue, #00d8ff);
    border-bottom-left-radius: 10px;
    filter: drop-shadow(0 0 6px rgba(0,216,255,0.55));
  }
  .map-corner-br {
    bottom: 14px; right: 14px;
    border-bottom: 2px solid var(--neon-pink, #ff2fb3);
    border-right: 2px solid var(--neon-pink, #ff2fb3);
    border-bottom-right-radius: 10px;
    filter: drop-shadow(0 0 6px rgba(255,47,179,0.55));
  }

  .map-card iframe {
    display: block;
    width: 100%;
    height: 100%;
    min-height: clamp(360px, 52vw, 520px);
    border: 0;
    border-radius: 20px;
  }

  @media (max-width: 900px) {
    .location-grid {
      grid-template-columns: 1fr;
      gap: 1.2rem;
    }
    .store-card {
      flex-direction: row;
      flex-wrap: wrap;
      align-items: center;
      gap: 1.2rem;
      padding: 1.4rem 1.5rem;
    }
    .store-card-head {
      flex: 1 1 240px;
    }
    .store-meta {
      flex: 1 1 280px;
      flex-direction: row;
      flex-wrap: wrap;
      gap: 1rem 1.6rem;
    }
    .store-meta li {
      flex: 1 1 130px;
    }
    .store-divider {
      width: 100%;
    }
    .store-cta {
      flex: 0 0 auto;
    }
    .map-card iframe {
      min-height: 380px;
    }
  }

  @media (max-width: 620px) {
    .location-one {
      padding: clamp(3.25rem, 12vw, 4.8rem) 1rem;
    }
    .location-header {
      margin-bottom: 1.6rem;
    }
    .store-card {
      flex-direction: column;
      align-items: stretch;
      padding: 1.4rem 1.3rem;
    }
    .store-meta {
      flex-direction: column;
      gap: 0.85rem;
    }
    .store-cta {
      width: 100%;
    }
    .map-card {
      border-radius: 20px;
    }
    .map-card iframe {
      min-height: 320px;
      border-radius: 18px;
    }
    .map-corner {
      width: 22px;
      height: 22px;
    }
    .map-corner-tl { top: 10px; left: 10px; }
    .map-corner-tr { top: 10px; right: 10px; }
    .map-corner-bl { bottom: 10px; left: 10px; }
    .map-corner-br { bottom: 10px; right: 10px; }
  }

  @media (max-width: 430px) {
    .location-one {
      padding-inline: 0.85rem;
    }
    h2 {
      font-size: clamp(2rem, 12vw, 2.8rem);
    }
  }
</style>
